{
  "name": "@ashe/conformance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Executable conformance suite for the ASHE specification. v0.1 ships the weightlessness gate (ADR-020). Implementations plug in via an adapter; the suite is the spec's runnable arm.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:example": "ASHE_CONFORMANCE_ADAPTER=./src/examples/structural-reference-adapter.ts vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
